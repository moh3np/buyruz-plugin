# قوانین اجباری توسعه افزونه تنظیمات بایروز (Buyruz Settings Plugin Rules)

هرگونه تغییر در این مخزن و فایل‌های افزونه بایروز باید بی‌قید و شرط مطابق قوانین `CONTRIBUTING.md` اجرا شود. این قوانین برای تمام هوش‌های مصنوعی و ایجنت‌ها اجباری و همیشگی است.

---

## 📌 ۱. اجبار افزایش نسخه در هر تغییر (Versioning Enforcement)
- هر تغییری (حتی کوچک یا اصلاح یک خط کد) **باید نسخه افزونه را به صورت `اصلی.فرعی.اصلاح` (مثلاً ۵.۲۵.۱) افزایش دهد.**
- استفاده مجدد از نسخه قبلی اکیداً ممنوع است.
- مقدار نسخه جدید باید **همزمان و به طور دقیق** در تمامی 4 مکان زیر به‌روزرسانی شود:
  1. هدر فایل `buyruz-settings.php` (کلید `Version`)
  2. ثابت `BRZ_VERSION` در فایل `buyruz-settings.php`
  3. بخش نسخه در فایل `README.md`
  4. ورودی جدید در فایل `CHANGELOG.md` با توضیح فارسی برای کاربر نهایی

---

## 📌 ۲. هشدار اجباری ابتدای تمام فایل‌ها (Mandatory Header Line)
تمام فایل‌های پروژه (جدید یا موجود) باید در ابتدای خود دارای این جمله هشدار باشند:
`// هشدار: پیش از هر تغییر، حتماً فایل CONTRIBUTING.md را با دقت کامل بخوانید و بی‌قید و شرط اجرا کنید و پس از اتمام کار تطابق را دوباره چک کنید؛ این جمله باید در تمام فایل‌ها (جدید و موجود) بدون استثنا افزوده و حفظ شود.`

---

## 📌 ۳. نگارش یادداشت تغییرات (Persian Changelog)
- در `CHANGELOG.md` آخرین تغییرات به زبان فارسی روان و قابل فهم برای مدیر سایت ثبت شود.

---

## 📌 ۴. ساخت اتوماتیک فایل ZIP (Auto Zip Build)
- پس از هر ویرایش کدی و ارتقای نسخه، ایجنت **موظف است به طور اتوماتیک و بدون نیاز به درخواست کاربر**، فایل زیپ بروز پروژه (`buyruz-wordpress-plugin.zip`) را در ریشه توسعه تولید/به‌روزرسانی کند.

---

## 📌 ۵. چک‌لیست انتشار
- پس از اعمال تغییرات، نسخه جدید در فایل‌ها به‌روز شده، `CHANGELOG.md` آپدیت شده، فایل `.zip` ساخته شده و تغییرات کامیت و پوش شوند.
